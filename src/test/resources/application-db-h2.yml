# h2 db
spring:
  h2:
    console:
      enabled: true
  datasource:
    hikari.jdbc-url: jdbc:h2:mem://localhost/~/testdb;MODE=MYSQL;DATABASE_TO_LOWER=TRUE
  jpa:
    hibernate:
      direct: org.hibernate.dialect.MySQL5InnoDBDialect
      ddl-auto: create-drop
